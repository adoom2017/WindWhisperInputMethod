# 风语完整开发计划

> 文档状态：已确认，M7 功能实现与自动化验收完成
> 规划版本：0.1  
> 日期：2026-08-24  
> 当前门禁：**M7 设置、状态与诊断已完成，进入本机使用确认；确认后进入 M8 兼容性、性能与可靠性**

## 1. 文档目的

本文是后续 AI Coding Agent 和人工开发者共同遵循的实施规范。它定义产品范围、架构边界、阶段任务、交付物、测试方法、风险与验收门禁。任何实现都应以本文为主线，并用 `docs/DECISIONS.md` 记录后续决策。

用户已于 2026-08-24 明确确认开始编码，并允许本机安装测试。M1—M7 已完成代码与自动化验收；M4 候选窗已由用户确认可见，M5 视觉反馈已完成迭代，M6 词库/双拼/辅码与 M7 设置/诊断均已通过自动化门禁。2026-08-25 又按用户原有 Rime 配置把默认方案升级为小鹤音形辅助码，并加入单按 Shift 中英文切换。

## 2. 产品目标

构建一个原生 macOS 中文输入法，满足以下核心目标：

1. 通过 InputMethodKit 成为标准 macOS 输入源，在常见 Cocoa 文本控件与主流应用中稳定工作。
2. 使用 librime 提供组合、候选、选词、翻页、用户词频、方案切换和部署能力。
3. 兼容 Rime schema 与词典生态，首版支持全拼、多种双拼，以及可配置的辅码输入。
4. 候选窗采用 AppKit 原生非激活面板；macOS 26 使用 `NSGlassEffectView`，较旧系统以 `NSVisualEffectView` 回退，呈现贴合系统的毛玻璃视觉。
5. 对用户输入保持本地优先：首版不上传按键、组合串、候选或用户词典。
6. 形成可复现的构建、安装、调试、测试和发布流程。

### 2.1 首版成功指标

- 在约定的最低 macOS 版本与最新稳定 macOS 上可安装、启用、切换和卸载。
- 在 TextEdit、Safari、Notes、Terminal/Xcode 编辑器和至少一个 Electron 应用完成输入闭环。
- 连续输入、退格、光标移动、候选选择、翻页、中英文切换无明显状态错乱。
- 全拼和至少三种双拼方案可用；辅码能缩小候选范围且不破坏纯双拼输入。
- 候选窗位置跟随插入点，多显示器、缩放、深浅色与减少透明度模式下可读。
- 崩溃、卡死、候选窗残留、marked text 残留均达到发布门禁要求。

## 3. 非目标与范围边界

首版不承诺：

- 云同步、账号体系、在线词库或联网联想。
- iOS/iPadOS 版本。
- 自研分词、语言模型或替代 librime 的输入算法。
- 完整的图形化 schema 编辑器。
- App Store 分发；输入法系统集成与分发路径需单独验证。
- 与所有第三方 Rime 配方零配置兼容。
- 在未明确授权前复制或重新分发许可证不兼容的词库。

## 4. 编码前必须确认的产品决策

以下项目在 M1 前由用户确认，未确认时不得用临时值扩散到工程：

| 编号 | 决策 | 建议默认值 | 影响 |
|---|---|---|---|
| D-01 | 产品显示名称 | 已采用“风语”；内部工程名保持 `RimeInputMethod` | 菜单、安装路径、资源命名 |
| D-02 | Bundle ID | 开发版采用 `com.shendongchun.inputmethod.rime.dev` | macOS 26 注册、签名、输入源 ID、升级兼容 |
| D-03 | 最低 macOS | 已采用 macOS 13 | API 可用性、测试矩阵 |
| D-04 | 架构 | 已采用 arm64 + x86_64 通用 Release | librime 产物、包体、CI |
| D-05 | 默认输入方案 | 已采用“小鹤双拼（音形辅码）”，纯音码保留可选 | 首次使用体验 |
| D-06 | 内置双拼集合 | 已采用自然码/小鹤/微软/智能 ABC | 资源与测试范围 |
| D-07 | 首版辅码规则 | 默认小鹤两键音码＋最多两位小鹤形码；保留 `;`＋全拼＋仓颉首码 | schema 设计、候选注释 |
| D-08 | 词库来源 | 已锁定官方 Rime 数据，见 `DATA_LOCK.json` | 合规、体积、更新机制 |
| D-09 | 安装范围 | 开发期已采用当前用户优先 | 权限、脚本与升级 |
| D-10 | 设置入口 | 菜单栏轻量设置或独立窗口 | target 与生命周期 |

## 5. 总体架构

```text
macOS 文本客户端
        │ NSEvent / IMKTextInput
        ▼
InputMethodKit Server
        │ 每个输入会话一个 Controller
        ▼
Input Controller ────────► Candidate Window Coordinator
        │                         │
        │ 规范化按键/上下文        └─► NSPanel + NSGlassEffectView / NSVisualEffectView
        ▼
Rime Service / Session Adapter
        │ librime C API
        ▼
librime Engine
        │
        ├─ Shared data：随包 schema、词典、OpenCC 配置
        └─ User data：用户配置、用户词典、部署产物、日志
```

### 5.1 生命周期所有权

- 应用级对象持有 `IMKServer`、全局 librime 初始化状态、配置快照和候选窗协调器。
- 每个 `IMKInputController` 对应一个 librime session；创建、激活、停用和销毁必须成对。
- librime 全局 `setup/initialize/finalize` 只在明确的进程生命周期节点执行。
- `get_context/get_status/get_commit` 获取的 librime 数据在成功读取后必须调用配对的 free API。
- UI 更新统一回到主线程；部署和大词典构建不能阻塞按键事件路径。

### 5.2 输入事件处理顺序

1. 接收 `NSEvent`，过滤系统快捷键与前端不应吞掉的修饰键组合。
2. 将 macOS keyCode、字符和 modifier flags 映射为 librime 可识别的 key symbol/mask。
3. 调用 librime 处理事件。
4. 按固定次序读取 commit、context、status。
5. 有提交文本时调用客户端 `insertText`；有组合时更新 marked text 与选择范围。
6. 将候选、页码、高亮索引、注释和 preedit 生成不可变 UI 快照。
7. 在主线程刷新、定位或隐藏候选窗。
8. 事件未被 librime 消费时返回给宿主应用。

任何实现必须避免同一个事件被提交两次、吞掉 Command 快捷键、或在客户端失效后继续发送文本。

## 6. 技术选型与约束

### 6.1 macOS 前端

- 语言：Swift；只有 C/C++ 互操作确有需要时使用 C 或 Objective-C++。
- UI：AppKit，不以 SwiftUI 承担候选窗关键渲染路径；设置页可在后续评估 SwiftUI。
- 系统框架：InputMethodKit、AppKit、Foundation；输入源注册使用 Apple 提供的 Text Input Source Services 能力，并隔离在安装器模块。
- 并发：按键处理保持同步、短路径；部署任务放到受控后台队列，并显式管理状态切换。
- 日志：统一结构化类别，默认不记录原始按键、完整组合串、提交文本和候选内容。

### 6.2 librime 集成

- 只通过稳定的 librime C API 与引擎交互，不从 Swift 直接依赖内部 C++ 类型。
- 创建窄接口 `RimeBridge`，负责结构体初始化、字符串编码、内存释放、API 可用性检查与错误归一化。
- 依赖版本必须锁定到明确 tag/commit；记录许可证和构建参数。
- 发布产物必须可移植，不能隐式依赖开发机上的 Homebrew 动态库。
- 在通用构建场景下分别验证 arm64 与 x86_64 slice，再合并并检查运行时依赖。
- 部署目录、共享数据目录、用户数据目录和日志目录必须显式定义且可测试。

### 6.3 Rime 数据设计

- `Resources/Rime` 只放可再分发且已记录来源/版本/许可证的数据。
- 用户修改放在用户数据目录；更新应用时不得覆盖用户 `.custom.yaml` 和用户词典。
- 预编译数据与源配置分离，明确首次部署、快速部署、全量部署和失败回滚策略。
- 双拼优先复用标准 Rime spelling algebra 与官方/社区 schema，不在 Swift 层转换双拼码。
- 辅码优先通过 schema 的 translator/filter/反查或派生词典完成；前端只展示候选注释和状态。
- 每个内置方案必须有固定输入样例与期望候选的回归测试。

## 7. 原生毛玻璃候选窗规范

### 7.1 窗口行为

- 使用不可抢夺输入焦点的 `NSPanel`，不激活应用、不打断宿主编辑状态。
- macOS 26 内容根视图使用 `NSGlassEffectView` clear 样式；旧系统使用 `NSVisualEffectView`，材质、混合模式和 active state 通过视觉样机验证。
- 窗口随插入点定位；优先读取 InputMethodKit 客户端提供的光标矩形。
- 超出当前屏幕可见区域时自动翻转或收缩；必须按候选窗所在屏幕而非主屏幕计算。
- 对 Retina、不同缩放比例、横排/竖排文本和全屏空间进行验证。

### 7.2 可访问性与系统设置

- 深色/浅色模式使用动态系统颜色。
- `Reduce Transparency` 开启时退化为高对比不透明背景。
- `Increase Contrast`、不同强调色与减少动态效果下保持可读和可操作。
- 字号、行高、圆角、内边距、阴影、动画时长统一由主题模型控制。
- 不能仅用颜色表达高亮；候选序号、焦点态和分页状态需清晰。

### 7.3 候选内容

- 支持横排与竖排策略，但首个里程碑可先交付一种布局。
- 展示候选序号、文本、可选注释/辅码、当前页和翻页能力。
- 候选点击、滚轮/触控板翻页与键盘选择产生同一语义动作。
- 候选模型使用快照，窗口不直接持有 librime 指针或 session 可变状态。

## 8. 工程目录蓝图

```text
RimeInputMethod/
├── README.md
├── DEVELOPMENT_PLAN.md
├── LICENSES/                         # M1 创建第三方许可证清单
├── App/                              # main、AppDelegate、IMKServer
├── Sources/
│   ├── InputController/              # IMKInputController 子类与事件适配
│   ├── RimeBridge/                   # C shim、Swift adapter、模型转换
│   ├── CandidateWindow/              # Panel/View/Layout/Theme/Coordinator
│   ├── Configuration/                # 路径、schema、用户偏好
│   ├── Installer/                    # 注册、启用、诊断、卸载
│   └── Shared/                       # 日志、错误、基础类型
├── Resources/
│   ├── Rime/                         # 许可确认后的 Rime 数据
│   └── Assets/
├── Tests/
│   ├── Unit/
│   ├── Integration/
│   ├── Fixtures/
│   └── Manual/
├── Vendor/                           # 锁定的 librime 构建产物/元数据
├── Scripts/                          # 可复现构建、安装、诊断、打包
└── docs/
```

不得为了“先跑起来”把所有逻辑塞进 `InputController`。事件映射、引擎桥接、候选模型、窗口布局和安装逻辑必须有清晰边界。

## 9. 组件接口规划

### 9.1 InputController

职责：接收系统事件、维护输入会话、更新 marked text/commit、把不可变候选快照交给 UI。

不得负责：解析 YAML、持久化主题、直接绘图、编译词典、拼接 dylib 路径。

### 9.2 RimeBridge

对 Swift 暴露概念接口：

- 服务：初始化、部署、结束、健康状态。
- 会话：创建、销毁、处理按键、清空/提交组合。
- 快照：commit、composition、menu、status、schema、options。
- 动作：选词、高亮、翻页、切换 schema/option。

桥接层统一处理 UTF-8 byte offset 到 Swift 字符索引/UTF-16 `NSRange` 的转换，并对所有 C 内存所有权写测试。

### 9.3 CandidateWindow

输入：`CandidateSnapshot`、插入点矩形、屏幕信息、主题与可访问性设置。  
输出：选择候选、翻页、移动高亮、关闭等语义动作。

窗口不能自行读取引擎；布局应可在无 InputMethodKit 的测试宿主中独立渲染和截图。

### 9.4 Configuration

建立三层优先级：内置默认值 < Rime/应用配置 < 用户覆盖。配置读取失败时使用安全默认值，并报告可操作的诊断信息。

## 10. 开发里程碑

每个里程碑必须独立提交并通过退出门禁。未经用户同意，不得跨阶段提前实现高风险功能。

### M0：规划与空目录（已完成）

任务：

- 建立目录蓝图和占位文件。
- 完成产品、架构、测试、决策和 AI 执行文档。
- 列出编码前待确认项。

退出门禁：用户审阅并明确回复允许开始编码。  
本阶段禁止：创建功能源文件、Xcode 工程、依赖下载、系统注册或安装。

### M1：最小 InputMethodKit 骨架（工程与免注销安装流程验收完成，待 TextEdit 人工确认）

任务：

- 创建 Xcode 工程和输入法 target。
- 固定 Bundle ID、连接名、controller class 和输入模式 ID 的唯一来源。
- 实现最小 `main`、`IMKServer` 和 controller 生命周期，只做可诊断的 passthrough。
- 增加 Debug 安装/卸载/重启输入法进程脚本。
- 本机开发安装使用本地签名与当前会话刷新；Developer ID 与 Apple 公证仅属于后续分发流程。

验收：输入源能安装、在系统设置中启用、切换后不吞键，并能完整卸载。  
不包含：librime、候选窗和实际中文转换。

### M2：librime 可复现集成（已完成）

任务：

- 选择并锁定 librime 版本与构建方式。
- 构建目标架构的可移植产物，检查动态依赖、rpath 与签名。
- 创建 C/Swift 窄桥接和 RAII/配对释放封装。
- 在测试宿主中完成初始化、部署、创建 session、输入、读取候选、提交、销毁。

验收：干净环境可重复构建；命令行测试闭环通过；无开发机 Homebrew 运行时依赖。

### M3：基本输入闭环（已完成）

任务：

- 实现按键与修饰键映射。
- 连接 session 生命周期与 InputMethodKit controller。
- 实现 marked text、selection range、commit、Escape、Return、Space、Backspace。
- 明确 Command/Control/Option 快捷键的传递规则。
- 处理激活、停用、焦点切换和失效 client。

验收：TextEdit 中完成稳定全拼输入；切换应用或输入源不残留组合与窗口。

已完成：进程级 Rime runtime、每 controller 独立 session、串行化引擎调用、按键映射、快捷键透传、marked text、commit、Backspace、Escape、Space、Return 和停用提交策略。Debug/Release 的 `--m3-smoke` 已验证生产前端更新器向 `IMKTextInput` 发送 marked text 与“你好”提交；本机热安装无需注销。

人工验证并入 M4：macOS 拒绝未授予辅助功能权限的 `osascript` 发送真实按键（错误 1002），因此由用户在 TextEdit 中完成真实输入门禁。

### M4：候选数据与基线窗口（已完成）

任务：

- 建立不可变 candidate/context/status 快照。
- 支持候选序号、选择、高亮和翻页。
- 先用最简单的非激活 AppKit 面板验证行为与定位；同时以系统 `IMKCandidates` 做对照测试（不作为最终 UI 承诺）。
- 增加多屏边界与插入点变化测试。

验收：键盘和鼠标选择一致；窗口永不抢焦点；页码和高亮与引擎一致。

已完成：不可变 candidate/context/status 快照接入；非激活 `NSPanel` 基线候选窗；候选序号、注释、页码和高亮；数字键、方向键、PageUp/PageDown 与鼠标语义选词；插入点定位、多显示器选择、下方优先、空间不足向上翻转和可见区域约束；停用、提交与错误路径隐藏窗口。

自动化：`Scripts/test-m4.sh Debug|Release` 覆盖候选展示模型、鼠标命中、非激活窗口配置、屏幕选择、边界定位，以及真实 librime 的候选高亮、翻页和选择提交。候选窗显示已由用户在实际输入中确认；详细记录见 `docs/M4_VALIDATION.md`。

### M5：原生毛玻璃视觉（实现与自动化验收完成，本机视觉门禁待确认）

任务：

- 引入 `NSGlassEffectView` / `NSVisualEffectView` 回退、动态系统色、圆角、阴影和主题 token。
- 完成横排布局，评估竖排布局是否纳入首版。
- 支持候选注释/辅码显示、自适应宽度与长文本截断。
- 覆盖深浅色、减少透明度、增强对比度、Retina、多屏和缩放。
- 动画必须短、可中断、不会延迟按键反馈。

验收：视觉截图基线通过；可访问性模式下信息清晰；快速输入无明显布局抖动。

已完成：macOS 26 以 `.clear` 的 `NSGlassEffectView` 呈现真正系统玻璃，并由该视图直接控制连续圆角，避免矩形窗口阴影产生尖角；macOS 13—15 使用 `.popover` / `.behindWindow` 的 `NSVisualEffectView` 回退。使用动态系统色和集中主题 token；首版确定为横排候选布局，短候选正文优先完整显示，注释/辅码与长文本按剩余宽度截断；页码、点击翻页和滚轮/触控板翻页接入统一 Rime 动作；降低透明度、增强对比度和减少动态效果提供明确降级。出现动画仅为可中断的 80ms 淡入，隐藏与输入状态更新不等待动画。

自动化：`Scripts/test-m5.sh Debug|Release` 覆盖主题 fallback、横排边界、长文本宽度分配、原生材质配置，以及 Aqua/Dark Aqua × 普通/无障碍模式的离屏视觉快照。竖排布局经评估不纳入首版，保留为后续可选布局；人工门禁见 `docs/M5_VALIDATION.md`。

### M6：Rime 词库、双拼与辅码（已完成）

任务：

- 建立第三方数据清单：来源、commit/tag、许可证、修改和更新方式。
- 接入获准分发的基础词库和用户词典。
- 接入确认的双拼集合，建立输入样例回归测试。
- 根据 D-07 设计辅码触发方式、码表、候选过滤和注释显示。
- 验证部署失败、schema 缺失、用户覆盖、升级不覆盖用户数据。

验收：每个内置方案都通过固定语料；辅码能稳定缩小候选；纯双拼行为不回退。

已完成：默认“小鹤双拼（音形辅码）”，内置纯音码小鹤、风语全拼、自然码、微软和智能 ABC 并保留仓颉兼容方案；数据来源、提交、许可证和本地修改均已锁定。默认小鹤方案采用“两键音码＋最多两位小鹤形码”，由用户原配置可复现生成 36,253 条；通用辅码仍可用 `;` 独立触发，按“完整拼音＋仓颉首码”生成 28,250 条索引。

自动化：`Scripts/test-m6.sh Debug|Release` 覆盖 6 套拼音方案固定语料、小鹤 `ni → nir → nirx → 你`、通用辅码缩小候选、候选注释、纯双拼回归、缺失方案拦截、用户 `.custom.yaml`/用户词典升级保护、数据锁和两类辅码词典复现。详细记录见 `docs/M6_VALIDATION.md`。

### M7：设置、状态与诊断（已完成）

任务：

- 提供方案切换、全/半角、简繁、候选方向和主题的最小设置入口。
- 提供重新部署、打开用户目录、查看脱敏诊断和恢复默认配置的入口。
- 状态变更在多 session 间行为一致，且不造成组合丢失。

验收：设置持久化、重启后恢复；错误提示可操作且不泄露输入内容。

已完成：使用 InputMethodKit 原生输入法菜单作为设置入口，不额外常驻一个菜单栏图标；支持小鹤、全拼、自然码、微软、智能 ABC 和仓颉方案切换，全/半角、简繁、横/竖候选排列，以及跟随系统/浅色/深色主题。设置保存到输入法自身 `UserDefaults`，新 session 自动应用，已有 session 在变更前先提交当前组合再同步，避免输入丢失。重新部署在后台执行，部署前释放 session、完成后自动重建；用户目录、脱敏诊断和只重置风语设置且保留用户词典/`.custom.yaml` 的恢复入口均已接入。

自动化：`Scripts/test-m7.sh Debug|Release` 覆盖持久化、损坏值安全回退、多 session 同步、组合保护、OpenCC 真实繁简候选转换、横竖排布局、菜单完整性、诊断脱敏和恢复默认值。详细记录见 `docs/M7_VALIDATION.md`。

### M8：兼容性、性能与可靠性

任务：

- 执行应用兼容矩阵与长时间输入测试。
- 测量冷启动、首次部署、普通按键 P50/P95、候选窗更新时间、内存和 CPU。
- 对 session 泄漏、C 内存释放、UTF-8/UTF-16 范围、快速焦点切换做专项测试。
- 修复候选窗残留、重复提交、吞快捷键、崩溃与卡死。

建议门禁（测量后可调整）：普通按键到 UI 状态更新 P95 < 16 ms；无部署时不出现主线程长任务；一小时压力测试无持续内存增长。

### M9：签名、安装与发布候选

任务：

- 明确当前用户/系统级安装方案、升级和回滚。
- 完成 Release 签名、嵌套 dylib 签名、notarization 评估与 Gatekeeper 验证。
- 生成许可证、版本清单、变更日志、已知问题和卸载说明。
- 在干净用户账户/干净机器执行发布验收。

验收：安装、首次启用、升级、回滚和卸载均有验证记录；发布包不依赖开发机环境。

## 11. 测试策略

### 11.1 单元测试

- macOS keyCode/modifier 到 Rime key 的表驱动测试。
- UTF-8 byte offset、Unicode scalar、grapheme cluster 与 UTF-16 `NSRange` 转换。
- librime 结构体初始化与所有权配对。
- candidate snapshot 映射、分页和高亮。
- 候选窗边界定位、屏幕翻转和主题 fallback。
- 配置优先级、缺失/损坏配置与迁移。

### 11.2 集成测试

- 使用固定 Rime fixture 从按键序列到候选/提交的确定性测试。
- 多 session 隔离、创建销毁、焦点切换和部署期间行为。
- 输入法进程重启后用户词典与设置保持。
- 错误 dylib、资源缺失、部署失败时安全降级。

### 11.3 手工兼容测试

- 原生：TextEdit、Notes、Safari 地址栏/网页输入框。
- 开发工具：Terminal、Xcode 编辑器与搜索框。
- 跨平台 UI：至少一个 Electron 应用。
- 特殊场景：密码框、安全输入、全屏、多显示器、远程桌面（若纳入支持）。
- 输入行为：快速输入、长句、Emoji/扩展字符、撤销重做、鼠标改光标、候选点击。

完整矩阵见 `docs/TEST_PLAN.md`。

## 12. 安全、隐私与数据

- 首版网络访问默认为零；若未来增加更新或云功能，必须单独获得确认并提供开关。
- 日志默认只包含事件类别、耗时、状态码和版本，不包含明文输入内容。
- 密码框/安全输入场景尊重系统行为，不尝试绕过 Secure Input。
- 用户目录权限按最小权限创建；安装脚本不得删除未知文件。
- 卸载默认保留用户词典，删除用户数据必须单独确认并可清楚预览目标路径。
- 第三方二进制与数据都记录来源、校验值、版本和许可证。

## 13. 构建、依赖与发布原则

- 构建入口必须脚本化并支持从干净 checkout 重现。
- 依赖锁定文件与构建元数据进入版本控制，二进制来源可追溯。
- Debug 与 Release 的数据目录、日志等级和签名策略分离。
- 安装、卸载和重启进程脚本使用明确绝对目标，执行前验证 bundle identifier。
- 任何删除操作不得使用宽泛路径或未校验变量。
- Release 前检查 bundle 内容、架构、动态依赖、签名链、资源许可证和用户数据迁移。

## 14. 风险登记表

| 风险 | 影响 | 预防/缓解 |
|---|---|---|
| InputMethodKit 文档有限且应用行为有差异 | 兼容性缺陷 | 早建最小骨架；真实应用矩阵；参考成熟开源前端但不照搬 |
| 输入事件映射错误 | 吞键、快捷键失效 | 表驱动映射；Command 路径专项测试；未消费事件透传 |
| marked text 范围换算错误 | 崩溃、光标错位 | 集中转换；覆盖 Emoji/组合字符；禁止散落 `NSRange` 计算 |
| librime C 内存管理错误 | 泄漏/崩溃 | 窄桥接；配对释放封装；压力测试与 Instruments |
| 候选窗抢焦点或定位错误 | 无法输入、体验差 | 非激活 Panel；多屏边界算法；焦点切换回归 |
| 首次部署耗时 | 切换输入法卡顿 | 后台部署、预编译资源、明确 maintenance 状态 |
| 第三方词库许可不兼容 | 无法发布 | M1 起建立许可证台账；未审查不进入 Resources |
| Homebrew 动态依赖泄漏 | 用户机器无法启动 | 自包含构建；检查 dylib/rpath；干净机验收 |
| 系统版本升级改变行为 | 回归 | 最低版 + 最新版 CI/手测；集中封装系统 API |
| 用户配置损坏 | 输入法不可用 | 验证、备份、回滚、安全默认 schema 与诊断入口 |

## 15. AI Coding Agent 执行协议

每次只执行一个已获准里程碑，并遵循：

1. 开始前阅读本计划、`docs/ARCHITECTURE.md`、`docs/DECISIONS.md` 和目标目录内的最新指令。
2. 检查工作区状态，保留用户已有改动；不得擅自重置或覆盖。
3. 先写该里程碑的短计划和验收命令，再改代码。
4. 修改范围保持最小，组件边界与所有权必须可解释。
5. 对新增行为同时添加相称测试和失败路径。
6. 完成后运行格式化、静态检查、单元/集成测试和阶段验收。
7. 更新 `docs/DECISIONS.md`、测试记录和 README 状态，不让文档落后于实现。
8. 报告实际验证结果、未验证项和剩余风险；不得把“应当可用”写成“已验证”。
9. 遇到 Bundle ID、许可、分发、数据删除或系统权限等决策时暂停并询问用户。
10. 不得未经允许自动安装到系统目录、切换当前输入源、结束用户进程或下载不明二进制。

## 16. 里程碑通用完成定义

某里程碑只有同时满足以下条件才算完成：

- 范围内功能和失败路径均已实现。
- 对应自动测试通过；手工项目有记录。
- 无新增编译警告、明显资源泄漏或敏感日志。
- 架构边界、所有权和线程规则未被破坏。
- 文档、决策记录、依赖清单和实际工程一致。
- 用户能从 README 重现该阶段结果。
- 未完成项被明确记录，没有用占位实现冒充完成。

## 17. 首版最终验收清单

- [ ] 可从干净 checkout 构建目标架构。
- [ ] 可安全安装、启用、切换、升级、回滚和卸载。
- [ ] 全拼、已确认的双拼与辅码测试全部通过。
- [ ] 用户词典、用户配置和重新部署流程可靠。
- [ ] marked text、commit、候选、翻页和选词在兼容矩阵中正确。
- [ ] Command 快捷键不被错误消费，Secure Input 行为符合系统预期。
- [ ] 候选窗不抢焦点，不越屏，不在切换后残留。
- [ ] 深浅色、减少透明度、增强对比度和多显示器可用。
- [ ] 普通输入性能达到经确认的门禁，无持续内存增长。
- [ ] Release 包动态依赖、签名、架构和许可证检查通过。
- [ ] 日志与网络行为符合隐私承诺。
- [ ] 已知问题和支持范围已向用户说明。

## 18. 官方参考资料

- Apple InputMethodKit：`IMKServer`、`IMKInputController`、`IMKServerInput`、`IMKTextInput`、`IMKCandidates`
- Apple AppKit：`NSPanel`、`NSVisualEffectView`、Accessibility Display Options
- librime 仓库与 `src/rime_api.h`
- librime macOS 构建说明 `README-mac.md`
- Rime 用户指南、定制指南与输入方案设计资料
- Rime 官方 macOS 前端 Squirrel（用于理解事件顺序、session 生命周期和候选窗边界；实现需保持本项目独立边界）
- rime-double-pinyin 输入方案仓库

实施时应固定具体链接、版本与访问日期到依赖/许可证清单，而不是长期跟随 `master`。

## 19. 当前停点

M1—M7 已完成实现。除 InputMethodKit 输入闭环、原生毛玻璃候选窗、全拼/双拼/辅码外，风语现已有默认小鹤音形、单按 Shift 中英文切换、可持久化的系统输入法菜单、跨 session 设置同步、真实繁简转换、横竖候选布局、后台重新部署和脱敏诊断；数据来源、许可证、生成方式与升级保护均已自动化验证。下一阶段仍为 M8 兼容性、性能与可靠性。
